CREATE TABLE users (
    id INTEGER PRIMARY KEY,
    username TEXT UNIQUE,
    password_hash TEXT

);

CREATE TABLE threads (
    id INTEGER PRIMARY KEY,
    title TEXT,
    genres TEXT,
    grade INTEGER,
    review TEXT,
    user_id INTEGER REFERENCES users

);
